services:

  coupon:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: fastapi_backend
    ports:
      - "8001:8000"
    volumes:
      - ./backend:/app
    environment:
      - PORT=8000
    restart: unless-stopped

